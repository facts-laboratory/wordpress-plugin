export type Use = 'bundlr' | 'warp' | 'arweaveWallet';
export interface DeployFactMarketInput {
    tags: {
        name: string;
        value: string;
    }[];
    attachTo: string;
    rebutTx?: string;
    use?: Use;
    position: 'support' | 'oppose';
}
export interface AttachFactMarketInput {
    tx: string;
    rebutTx?: string;
    use?: Use;
    position: 'support' | 'oppose';
}
export declare function attachFactMarket(input: AttachFactMarketInput): Promise<{
    tx: string;
} | {
    error: string;
}>;
