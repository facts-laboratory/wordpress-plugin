import Transaction from 'arweave/node/lib/transaction';
/**
 * Gets arweave config and fetches a tx.
 *
 * @author mogulx_operates
 * @export
 * @param {string} tx
 * @return {*}  {Promise<Transaction>}
 */
export declare function getTx(tx: string): Promise<Transaction>;
